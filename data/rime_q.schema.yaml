# Based on rime-ice; the complete pinned upstream source accompanies the application.
__include: rime_ice.schema:/

schema:
  schema_id: rime_q
  name: Rime Q
  version: "0.3.1"
  author: [Rime Q contributors, Dvel]
  description: 简体全拼，离线输入。
  dependencies: [melt_eng, radical_pinyin]

# Keep the input-related capabilities. The host provides its own presentation.
engine:
  translators:
    - punct_translator
    - script_translator
    - lua_translator@*date_translator@date_translator
    - lua_translator@*q_lunar@lunar
    - lua_translator@*uuid@uuid
    - table_translator@custom_phrase
    - table_translator@melt_eng
    - table_translator@cn_en
    - table_translator@radical_lookup
    - lua_translator@*unicode
    - lua_translator@*number_translator
    - lua_translator@*calc_translator
  filters:
    - lua_filter@*q_corrector
    - reverse_lookup_filter@radical_reverse_lookup
    - lua_filter@*autocap_filter
    - lua_filter@*v_filter
    - lua_filter@*reduce_english_filter
    - simplifier@emoji
    - simplifier@traditionalize
    - lua_filter@*search@radical_pinyin
    - uniquifier

# Share personal learning with the sentence-optimization schema.
translator:
  dictionary: rime_ice
  user_dict: rime_q
  enable_user_dict: true
  enable_sentence: true
  enable_completion: true
  enable_correction: true
