# Rime Q 项目约定

本文件适用于整个仓库。用户在当前会话中的明确要求优先于本文件。规则来自已确认的产品需求和本机验证；尚未证实的排查假设不要写成结论。

## 产品方向

- 名称为 **Rime Q**，仓库为 `asmoyou/rime-Q`。简介保持简洁：简洁、流畅、离线的中文输入法。
- 以高性能、输入舒适和可靠性为优先，基于 librime，独立开发界面与体验。参考 RIMES 的实现经验，但使用自己的标识、安装路径和个人数据目录。
- 吸收 rime-ice、万象及优质网络词库的长处。引入资源前记录来源、版本、校验值和许可，保留第三方声明；不要把未经整理的词库叠加数量当成体验提升。
- 目标平台是 macOS、Windows、Linux，当前先把 macOS 的真实安装与输入测试通。跨平台核心构建通过，不代表对应客户端已经实现。
- Mac 必须能通过一个独立安装包使用，内置引擎和所需资源，不要求用户另外安装鼠须管。
- 日常输入保持离线。更新检查由用户主动触发，不上传输入内容，不默认后台检查、下载或安装更新。

## 安装与启用

- 发布安装路径固定为 `/Library/Input Methods/RimeQ.app`；开发安装可用 `~/Library/Input Methods/RimeQ.app`。同一标识只保留一个实际安装。
- Bundle ID 为 `com.asmoyou.inputmethod.RimeQ`，简体模式为 `.Hans`。禁止残留旧 RIMES 标识。
- `InputMethodConnectionName` 与运行时统一为 `com.asmoyou.inputmethod.RimeQ_Connection`。构建与启用预检必须拒绝旧的短连接名。GUI 预览使用独立 Bundle ID，移除输入源声明；生产构建模板和预览退出后均按确切路径取消临时 LaunchServices 注册，避免污染现用输入法。
- PKG 必须禁用 `BundleIsRelocatable`。旧包曾被 Installer 安装到已移动的 `.payload` 排查目录，不能只相信安装器的成功提示。
- 默认构建只保留 PKG；临时 `.app` 打包后删除。备份用 TAR 等归档，不能靠改后缀来隐藏应用；`.payload` 等目录仍可能被系统识别。
- 使用递增的 `CFBundleVersion`，包含正确的名称、本地化、图标和 PkgInfo，并检查实际签名与打包结果。
- 设置页、“关于 Rime Q”和命令行版本输出使用同一份 Bundle 版本信息，同时保留构建号以区分开发预览。安装器读取实际已装应用，自动区分首次安装、升级和同版修复，不只依赖安装收据；GUI 预检和写入前检查都要拒绝版本或构建降级。
- 安装后的启用由 **LaunchServices 正常启动的用户应用**执行，例如 `open -n -g .../RimeQ.app --args --complete-install`。本机已证实，从安装脚本直接运行注册命令可能返回成功但没有真正启用。
- 分开验证应用落盘、注册、父输入法启用、子模式启用、实际文本框输入。`TISEnableInputSource` 返回 0、子模式默认 `enabled=true` 或程序可以启动，都不等于整个输入法可用。
- 安装/登录重试完成注册后，新进程应继续初始化引擎和 IMK 输入服务；不能退出旧进程后只写“已启用”再退出安装进程。通过限定版本、路径与随机请求标识的进程应答检查服务状态；该检查仍不能代替真实宿主输入验收。保留带时间、构建号和进程号的安装生命周期日志，不记录输入内容。
- 本机 0.2.3 已复现启用记录存在、菜单与客户端却不同步；仅重新注册无效。当前会话内切离 Rime Q、依次停用子模式和父输入法，再注册并启用后，用户确认恢复，输入进程未重启。安装/修复使用这条刷新路径，失败时仍尝试恢复启用；登录重试不重复停用。不得据此声称已确定 macOS 内部缓存失效原因。
- 成功路径在后台完成启用，**不再弹“已启用”确认，不自动打开键盘设置，不要求注销或重启**。
- 只有未启用或发生错误时才显示必要提示。待启用默认按钮是“稍后处理”；注销必须由用户明确选择，并进入系统正常确认，不强制结束会话。
- 待启用可以安排下次登录重试一次；成功或重试完成后清理自己的任务，避免反复弹窗。日志只记录安装状态，不记录用户输入。
- 注销和重启是不同操作。不要在根因未明时将注销描述为必需步骤，也不要把注销后的结果当成清理配置成功的证明。

## 菜单、更新与卸载

- 系统输入法菜单提供设置、个人数据文件夹、使用说明、检查更新和卸载入口，避免重复的成功提示和无意义的操作步骤。
- 候选栏采用原生半透明材质，文字本身保持清晰，并遵循系统的减少透明度设置。按当前页编号、候选文字、实际注释和必要边距测量宽度，不给一两个字保留大块固定空白；长句限制宽度，注释不覆盖正文。
- 动态皮肤仅响应已有输入事件，不新增全局键盘监听；闲置、候选隐藏及“减少动态效果”时停止动作。装饰不得覆盖候选或改变数字键、鼠标选词的索引。
- 动态装饰趴在候选栏上边缘外侧，不增加候选内容区高度或最小宽度。外置装饰随候选窗口显示、隐藏及避让屏幕边缘，鼠标事件透传。皮肤页分“简洁皮肤”和“高级皮肤”，后者用独立动态预览卡片。
- 设置、关于和离线帮助提供“免费开源、官方版本无需购买”及项目主页/官方下载入口：`https://github.com/asmoyou/rime-Q`。保持许可条款原文，不将免费官方下载说明写成禁止再分发的条款。
- 雾凇 Lua 功能须验证真实候选与上屏；文件随包不代表脚本已加载。农历资源显式从共享目录读取；自定义词库配置在升级后异步迁移输入配置，保留词表选择及学习记录。
- InputMethodKit 菜单动作必须接收 sender 参数，实际控制器需要响应相同 selector；菜单显示后还要逐项验证点击行为。
- 检查更新要区分存在新版本、没有新版本、尚无公开发布版本及请求失败，不把网络失败或没有 Releases 说成“已是最新”。
- 随包提供可离线打开的说明页，包含使用方法、安装异常、更新、数据备份及手动卸载步骤。
- 卸载前明确确认、切换到其他输入法并停用自己的输入源。系统目录的卸载走 macOS 管理员认证，取消或失败时尝试恢复输入源状态。
- 卸载默认保留个人词库和学习记录。个人数据位于 `~/Library/Application Support/RimeQ/rime`；不要删除或改写鼠须管、RIMES 或其他输入法的数据。
- 特权卸载操作只能针对经过路径和 Bundle ID 校验的 Rime Q 应用。不得清理整个 Input Methods 目录、整个用户 Library 或其他应用配置。
- 系统菜单可能同时显示输入法切换项和当前输入法的分组标题。看到同名文字时先核对条目属性和标识，不直接断言重复安装。

## 开发与验证

- 优先修复真实输入、安装、启动和数据保留问题。原生界面截图、模拟客户端或引擎测试不能代替真实宿主验收。
- 必测首次启动及切换后的首键、全拼组合、空格和数字选词、取消、中英文切换及焦点变化。引擎未就绪时不能吞掉按键。
- 性能结论注明机器、范围、样本和缓存条件；区分引擎耗时与包含系统/UI 的端到端耗时，不用合成基准宣称已经优于其他输入法。
- 构建：`python3 scripts/build_macos.py --universal --smoke`。已有资源缓存时可加 `--reuse-resources`。
- 开发安装才使用 `--keep-app` 和 `scripts/install_macos.py`，避免给正式 PKG 留下第二份可发现的应用。
- 运行与修改相关的检查：成品包规则、签名、引擎、安装状态/重试、更新版本比较及菜单回调。已通过且未受修改影响的测试不反复执行。
- CI 应实际安装 PKG，验证启用结果或明确的待启用处理，再测试引擎和卸载保留数据。CI 中的待启用状态要如实报告，不等同于已验证立即可用。
- 真实输入测试使用独立空白文档，每次发键前确认前台应用和文档，避免向用户其他窗口输入。不要在用户机器上为测试卸载而移除当前可用输入法。
- 在 `docs/VALIDATION.md` 更新已通过、未通过、复现步骤和实际证据。不要提前写“已修复”“已可用”或把临时假设写成根因。

## 协作与权限

- 每项权限说明写清申请方、具体用途、触发时机和拒绝后的影响。优先减少不必要的请求，不能用模糊文案要求“全部允许”。
- 用已有的安装介绍页、下载说明和离线帮助解释授权，不给成功路径再加一层确认弹窗。系统支持的用途说明键要本地化；Apple 安装器自己的下载目录提示不能由应用改写，且可能先于包内介绍页出现。
- 新版之间的更新优先使用自身的正常退出通知；较早版本的 Apple events 兼容操作必须有明确用途说明。不把开发工具的自动化或截屏权限写成终端用户的安装要求。
- 已经授权的开发、修复和验证继续执行，不重复询问相同许可；系统原生认证由用户本人完成，不索取密码。
- 尊重系统权限，不通过关闭 SIP、绕过 TCC 或反复强写受保护偏好来解决安装问题。
- 不输出环境变量、令牌或未经筛选的完整系统安装日志。安装器可能记录启动环境；排查时先过滤环境行和凭据，启动外部 GUI 应用时避免传递开发环境中的密钥。
- 遇到失败先看实际安装目录、系统日志、注册状态和 UI，避免反复重装。一次只改变可验证的因素，保留相关备份。
- 文档和用户提示使用简洁中文。先说明实际结果和下一步，明确区分事实、推断与尚待验证的部分。
